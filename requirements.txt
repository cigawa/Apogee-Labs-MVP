# Core (install cleanly everywhere)
streamlit>=1.30
anthropic>=0.40
scikit-learn>=1.3
pandas>=1.5

# CAD engine — installs cleanly on Linux/macOS/Windows-x64.
# On Windows ARM it may fail to build (no wheel). If so, either:
#   (a) run the app on Streamlit Community Cloud (Linux), or
#   (b) comment cadquery out — the app still runs (CAD disabled, profile+report work).
cadquery>=2.4

# RAG (LlamaIndex). The app falls back to TF-IDF if these are absent or if the
# knowledge_base/ folder is empty, so they are optional until you add documents.
# Uses HOSTED embeddings (OpenAI by default) — lightweight, no torch, fits the
# Streamlit Community Cloud free-tier memory limit. Needs OPENAI_API_KEY set in
# Streamlit secrets. To use local/private embeddings instead, add
# llama-index-embeddings-huggingface and set APOGEE_EMBED_BACKEND=local (only on
# a big-memory host like Hugging Face Spaces — torch will blow the free tier).
llama-index-core>=0.11
llama-index-readers-file>=0.2
llama-index-embeddings-openai>=0.2
# Optional alternative (Anthropic's recommended embeddings):
# llama-index-embeddings-voyageai>=0.2
